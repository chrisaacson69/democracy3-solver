# Machine-specific data path (mirrors the vault's local-paths philosophy)
config.toml

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/
build/
dist/

# built explorer page (regenerate with scripts/build_explorer.py)
explorer.html
explorer_model.json

# Savegames are Positech mission data + full game state — keep them local.
# Any Democracy 3 playthrough can produce one; see README for what the scripts expect.
tests/fixtures/*.xml

# built pages (regenerate with build_explorer.py / build_atlas.py)
atlas.html
